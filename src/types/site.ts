/**
 * Footer labels sourced from CMS translations.
 */
export interface SiteFooterLabels {
  contactTitle: string;
  copyright: string;
  followUsTitle: string;
  privacyPolicy: string;
  termsOfService: string;
}

/**
 * Header navigation item.
 */
export interface SiteNavItem {
  href: string;
  label: string;
}

